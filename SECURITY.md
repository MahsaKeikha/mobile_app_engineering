# Security Policy

F41 is a reference implementation and does not authorize production release decisions. Report vulnerabilities through a private GitHub security advisory when available rather than a public issue.

Security-sensitive changes should include tests for authentication, privacy, data handling, dependency risk, rollback behavior, and fail-closed release gates. Never place secrets, signing keys, certificates, production credentials, or private user data in examples, fixtures, logs, issues, or pull requests.
